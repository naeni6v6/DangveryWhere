// The mobile app initializes profiles, characters and favorites from the same
// browser storage as /web. Render its first frame with that state already loaded.
// This does not affect the server-rendered PC pages.
export const ssr = false;
