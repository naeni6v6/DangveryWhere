# 웹·모바일 우선 개선 4건 반영 기록

대상: 2026-09-20 웹 심사의 R01–R04. 로컬 소스에 반영했으며, 배포 작업은 포함하지 않았다.

## 반영 결과

| 항목 | 변경 | 검증 결과 |
| --- | --- | --- |
| R01. 해피플레이스 중복 | 네이버 플레이스의 상호·주소에 맞는 ‘속초애견펜션 양양해피플레이스’로 통합했다. 기존 두 ID의 사진·찜·상세 주소를 대표 ID로 연결한다. | 통합 목록은 516곳에서 515곳으로 변경. 예전 ID의 웹·모바일 상세 링크가 대표 장소로 열린다. 두 자료의 사진 6장이 연결된다. |
| R02. 맞춤 조건 초기화 | 검색어·카테고리·체중 정보 필터·반려견 조건 적용 여부를 브라우저에 저장하고 복원한다. 실제 적용 상태로 목록 제목과 조건 칩을 표시한다. | 소형견 10kg과 두 필터를 적용한 53곳이 웹·모바일 새로고침 및 화면 전환 후 유지된다. 프로필이 없거나 저장 값이 잘못된 경우도 단위 테스트로 확인했다. |
| R03. 종료된 외부 링크 | 종료된 modoo.at 링크 8개 중 7개를 동일 업체의 네이버 플레이스로 변경했다. 발렌타인애견펜션의 종료된 직접 링크는 제거하고 검색 경로를 표시한다. 해피플레이스도 확인한 플레이스로 교체했다. | 직접 링크 목록의 modoo.at은 0개. 링크 생성 스크립트에서도 종료 도메인을 제외하고 검수 결과를 적용한다. 목적지에 맞게 ‘네이버 플레이스 보기’ 또는 ‘네이버에서 장소 찾기’로 버튼을 구분한다. |
| R04. 필터가 목록 공간을 차지 | 웹 고급 필터를 대화상자로 옮기고 결과 수·적용 버튼을 제공한다. 닫으면 조건 칩만 남긴다. 모바일은 적용 후 결과 패널을 펼친다. | 웹 1280×720에서 목록 높이가 필터 적용 시 약 224px로 확보됐다. 기존 심사에서 필터를 펼친 상태는 약 32px였다. 모바일 390×844에서 필터 적용·새로고침·결과 탐색, 360×640에서 통합 장소 상세를 확인했다. |

해피플레이스의 체중 규정은 자료가 일치하지 않는다. 한국문화정보원 자료의 **10kg 미만**을 보수적으로 적용하되, 강원 반려동반관광 자료에는 **13kg**으로 기재됐다는 안내와 업체 확인 요청을 함께 표시했다. 체중 제한과 충돌하던 ‘제한사항 없음’ 표시는 제거하고 원본 자료는 보존했다. 네이버 플레이스에서 현재 체중 규정이 확인된 것으로 표시하지 않는다.

발렌타인애견펜션은 업체명 검색 결과가 없고 주소 검색에는 주소만 나왔다. 이 결과만으로 폐업을 단정하지 않았으며, 장소 자체가 아닌 종료된 직접 링크를 제거했다.

## 링크 확인 근거

2026-09-20 네이버 지도 검색과 업체 상세에서 상호·주소·전화번호를 대조했다. 도그스테이블·잠빌펜션의 전화번호, 강이네하우스의 주소와 지도 위치도 확인 결과에 맞췄다. 확인한 주소와 근거는 [검수 데이터](../src/lib/data/reviewedPlaceLinks.json)에 저장했다. 이 날짜는 링크 대조일이며, 모든 업체 동반 규정을 재확인한 날짜는 아니다.

## 검사

- `npm run check`: 오류 0개, 경고 0개.
- `npm test`: 19개 파일, 164개 테스트 통과.
- 마지막 체중 규정 안내 수정 후 관련 테스트 재실행: 3개 파일, 40개 테스트 통과.
- `npm run build`: 별도 검증 디렉터리에서 Cloudflare 어댑터 단계까지 통과. 실행 중인 개발 서버의 생성 파일 잠금을 피하기 위해 별도 소스 복사본을 사용했다.
- `git diff --check`: 통과.

브라우저 검증에는 별도 로컬 주소의 임시 반려견 프로필을 사용했다. 그 빌드에는 지도 키를 복사하지 않아 지도 배경은 안내 화면이며, 필터·목록·상세·화면 크기 검증에 사용했다. 기존 찜 ID의 변환은 단위 테스트로 확인했고, 실제 계정 DB의 찜 추가·삭제는 이번 검증에서 실행하지 않았다.

화면 기록: `C:/DangveryWhere/docs/fixes-2026-09-20/`의 `web-filter-dialog.png`, `mobile-filters-restored.png`, `mobile-merged-place.png`.
